# 本地开发时直接指向后端本地地址，和后端 context-path 保持一致
VITE_BASE_API = "http://localhost:9099/play2"

# 开发环境启用 eruda 调试工具。若不启用，将 true 修改为 false 或其他任意值即可
VITE_ENABLE_ERUDA = "false"

# 本地开发打包路径
VITE_PUBLIC_PATH = "/"
